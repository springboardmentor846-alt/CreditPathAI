# CreditPathAI – FastAPI Service (Milestone 5)

This folder contains the FastAPI application used to serve the trained loan default prediction model.

## How to Run the API

1. Install dependencies:
```bash
pip install -r requirements.txt

uvicorn app:app
http://127.0.0.1:8000/docs

